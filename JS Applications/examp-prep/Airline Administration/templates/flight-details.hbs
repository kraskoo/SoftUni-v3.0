{{> header}}
<section id="viewFlightDetails">
	<div class="ticket-area">
		<div class="ticket-area-left">
			<img src="{{flight.image}}" alt="">
		</div>
		<div class="ticket-area-right">
			<h3>{{flight.destionantion}}</h3>
			<div>from {{flight.origin}}</div>
			<div class="data-and-time">
				{{flight.date}} {{flight.departureTime}}
				{{#if flight.isCreator}}
				<a href="#/edit/flight/{{flight._id}}" class="edit-flight-detail"></a>
				{{/if}}
			</div>
			<div>
				{{flight.seats}} Seats ({{flight.cost}} per seat)
			</div>
		</div>
	</div>
</section>
{{> footer}}